<div class="nav">
    <a class="logo">AppleForum</a>
    <a href="/write">글쓰기</a>
    <a href="/list">리스트</a>


    <% if (user) { %>
        <a href="/logout">로그아웃</a>
        <h4><%=user.username %>님 환영합니다</h4>
      <% } else { %>
        <a href="/login">로그인</a>
      <% } %>
</div> 