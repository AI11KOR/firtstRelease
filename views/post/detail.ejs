<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main.css" />
    <title>Document</title>
</head>
<body class="grey-bg">

    <%- include('../nav.ejs') %>
    
    <div class="detail-bg">
        <h4><%= post.title %></h4>
        <p><%= post.content %></p>   
        <% if (post.imageURL) { %>
            <img src="<%= post.imageURL %>" style="width:200px;height:200px;border-radius:50%;object-fit:cover;"/>
        <% } else { %>
            <div style="width:200px;height:200px;border-radius:50%;border:1px solid #eee;"></div>
        <% } %>
        
    </div> 
</body>
</html>